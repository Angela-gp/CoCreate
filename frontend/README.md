# Frontend

`src/App.tsx` is the interactive CoCreate product surface. The framework route in `app/page.tsx` re-exports it so the repository keeps the requested frontend/backend/program separation without duplicating product logic.

