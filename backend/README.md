# Backend

This directory contains the event normalization and campaign read-model layer used by an indexer or API worker. The demo UI uses deterministic local fixtures so it remains reliable during presentations.

